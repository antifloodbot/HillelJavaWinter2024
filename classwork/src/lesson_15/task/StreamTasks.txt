Преобразование в коллекцию:
Создай поток из списка чисел.
Преобразуй его в коллекцию типа Set и верни результат.

Фильтрация и сортировка строк:
Создай поток из списка строк.
Отфильтруй строки, содержащие букву "Java", и отсортируй их по длине.

Конвертация в массив строк:
Создай поток из списка строк.
Преобразуй его в массив строк.

Группировка по длине строки:
Создай поток из списка строк.
Сгруппируй строки по их длине и верни карту.

Объединение и фильтрация чисел:
Создай два потока из двух разных списков чисел.
Объедини их и оставь только уникальные числа.

Фильтрация и преобразование объектов:
Создай поток из списка объектов Animal (с полями имя и возраст).
Отфильтруй только тех животных, возраст которых больше 3 лет, и преобразуй их в список имен.

Объединение и фильтрация строк:
Создай два потока из двух разных списков строк.
Объедини их и оставь только те строки, которые содержат букву "A".

Сортировка чисел в обратном порядке:
Создай поток из списка чисел.
Отсортируй его в обратном порядке.

Преобразование в строку с разделителем:
Создай поток из списка объектов City (с полями название и страна).
Преобразуй их в одну строку, разделяя названия городов запятой.

Фильтрация объектов и преобразование в Map:
Создай поток из списка объектов Product (с полями название, цена и категория).
Отфильтруй продукты с ценой больше 100 и преобразуй их в карту, где ключ - название, а значение - категория.

Пропуск и суммирование чисел:
Создай поток из списка чисел.
Пропусти первые 2 числа и сложи оставшиеся.

Фильтрация и преобразование в строку:
Создай поток из списка объектов Person (с полями имя, возраст и пол).
Оставь только женщин старше 25 лет и преобразуй их в строку вида "Имя (возраст)"

Группировка по первой букве:
Создай поток из списка слов.
Сгруппируй их по первой букве и верни карту.

Фильтрация и подсчет элементов:
Создай поток из списка строк.
Оставь только строки, содержащие слово "Stream", и подсчитай их количество.

Преобразование в набор уникальных символов:
Создай поток из списка строк.
Преобразуй каждую строку в набор уникальных символов.

Фильтрация и ограничение чисел:
Создай поток из списка чисел.
Оставь только положительные числа и верни первые 4.

Генерация случайных чисел:
Создай поток, который генерирует 5 случайных чисел от 1 до 100.

Фильтрация и преобразование объектов:
Создай поток из списка объектов Movie (с полями название и рейтинг).
Оставь только те фильмы, у которых рейтинг выше 8.

Преобразование в Map с подсчетом:
Создай поток из списка слов.
Преобразуй его в карту, где ключ - слово, а значение - количество раз, которое это слово встречается в списке.

Фильтрация и преобразование объектов:
Создай поток из списка объектов Product (с полями название и цена).
Оставь только те продукты, у которых цена меньше 50, и преобразуй их в список названий.

Объединение и фильтрация объектов:
Создай два потока из двух разных списков объектов Employee (с полями имя, возраст и должность).
Объедини их и оставь только тех сотрудников, у которых должность "Разработчик".

Преобразование в Map с фильтрацией:
Создай поток из списка объектов Customer (с полями имя, возраст и активен).
Преобразуй его в карту, где ключ - имя, а значение - возраст, только для активных клиентов.

Фильтрация и суммирование чисел:
Создай поток из списка чисел.
Оставь только четные числа и сложи их.

Объединение и фильтрация строк:
Создай два потока из двух разных списков строк.
Объедини их и оставь только строки, длина которых не превышает 10 символов.

Преобразование в Set:
Создай поток из списка слов.
Преобразуй его в множество (Set).

Фильтрация и преобразование в верхний регистр:
Создай поток из списка строк.
Оставь только те строки, которые содержат букву "B", и преобразуй их в верхний регистр.

Сортировка объектов по нескольким полям:
Создай поток из списка объектов Product (с полями название, цена и категория).
Отсортируй их сначала по категории, а затем по цене в порядке убывания.

Фильтрация и проверка условия:
Создай поток из списка чисел.
Оставь только те числа, которые делятся и на 3, и на 5.

Преобразование в строку и фильтрация:
Создай поток из списка объектов City (с полями название и страна).
Преобразуй их в строку, содержащую только города из определенной страны.

Группировка и подсчет элементов:
Создай поток из списка объектов Product (с полями название и категория).
Сгруппируй их по категории и подсчитай количество продуктов в каждой категории.

Объединение и фильтрация чисел:
Создай два потока из двух разных списков чисел.
Объедини их и оставь только отрицательные числа.

Фильтрация и подсчет символов:
Создай поток из списка строк.
Оставь только те строки, длина которых больше 5 символов, и подсчитай общее количество символов в этих строках.

Преобразование в Map с условием:
Создай поток из списка объектов Student (с полями имя, оценка и активен).
Преобразуй его в карту, где ключ - имя, а значение - оценка, только для активных студентов.

Фильтрация и подсчет элементов:
Создай поток из списка строк.
Оставь только строки, начинающиеся с буквы "P", и подсчитай их количество.

Объединение и фильтрация объектов:
Создай два потока из двух разных списков объектов Animal (с полями имя и возраст).
Объедини их и оставь только тех животных, у которых возраст меньше 5 лет.

Фильтрация и проверка условия:
Создай поток из списка строк.
Оставь только те строки, которые содержат только буквы (без цифр и символов).

Преобразование в Map и сортировка:
Создай поток из списка объектов Person (с полями имя, возраст и страна).
Преобразуй его в карту, где ключ - страна, а значение - список людей, отсортированных по возрасту.

Фильтрация и преобразование в Set:
Создай поток из списка чисел.
Оставь только уникальные числа и преобразуй их в множество (Set).

Сортировка строк по длине:
Создай поток из списка строк.
Отсортируй строки по их длине.

Преобразование в строку и фильтрация:
Создай поток из списка объектов Product (с полями название, цена и активен).
Преобразуй его в строку, содержащую только активные продукты.

Фильтрация и ограничение чисел:
Создай поток из списка чисел.
Оставь только числа в интервале от 20 до 50 и верни первые 3.

Преобразование в Set с фильтрацией:
Создай поток из списка строк.
Преобразуй его в множество (Set), оставив только строки, начинающиеся с буквы "M".

Фильтрация и преобразование в Map:
Создай поток из списка объектов Book (с полями название, автор и год выпуска).
Оставь только книги, выпущенные после 2000 года, и преобразуй их в карту

Объединение и фильтрация строк:
Создай два потока из двух разных списков строк.
Объедини их и оставь только те строки, которые содержат букву "E".

Преобразование в Map и фильтрация:
Создай поток из списка объектов City (с полями название и население).
Преобразуй его в карту, где ключ - название города, а значение - население, только для городов с населением больше 1 миллиона.

Фильтрация и преобразование в строку:
Создай поток из списка объектов Person (с полями имя, возраст и пол).
Оставь только мужчин старше 30 лет и преобразуй их в строку вида "Имя (возраст)".

Сортировка и ограничение:
Создай поток из списка чисел.
Отсортируй его по возрастанию и верни последние 5 элементов.

Преобразование в массив с фильтрацией:
Создай поток из списка объектов Animal (с полями имя и вид).
Преобразуй его в массив, оставив только тех животных, которые относятся к виду "Млекопитающие".

Фильтрация и подсчет элементов:
Создай поток из списка строк.
Оставь только те строки, которые заканчиваются на букву "s", и подсчитай их количество.

Объединение и фильтрация объектов:
Создай два потока из двух разных списков объектов Car (с полями марка и цена).
Объедини их и оставь только те автомобили, цена которых меньше 25000.